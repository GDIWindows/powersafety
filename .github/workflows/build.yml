name: Build APK

on:
  push:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 120
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Setup Java
      uses: actions/setup-java@v5  # 升级到 v5
      with:
        distribution: 'temurin'
        java-version: '17'
    
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'  # 改用更稳定的 3.11
    
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y git zip unzip
        pip install --upgrade pip
        pip install buildozer cython
    
    - name: Build APK
      run: |
        # 1. 创建字体目录
        mkdir -p fonts
        
        # 2. 创建 buildozer.spec
        cat > buildozer.spec << 'EOF'
[app]
title = 电力安全题库
package.name = powersafety
package.domain = org.example
version = 1.0.0
source.dir = .
source.include_exts = py,png,jpg,kv,atlas,ttf,ttc,otf,xml,json
source.main = main.py
android.permissions = INTERNET,READ_EXTERNAL_STORAGE,WRITE_EXTERNAL_STORAGE,CAMERA
android.api = 31
android.minapi = 21
android.sdk = 33
android.ndk = 28c
android.ndk_api = 21
android.archs = armeabi-v7a,arm64-v8a
requirements = python3,kivy,pillow,pytesseract,requests,android,plyer
android.allow_backup = True
android.app_name = 电力安全题库
android.display_name = 电力安全题库
fullscreen = 0
log_level = 2
android.accept_sdk_license = True

[buildozer]
log_level = 2
warn_on_root = 1
EOF
        
        # 3. 修复字体路径
        sed -i 's|C:/Windows/Fonts/simsun.ttc|fonts/NotoSansCJK.ttc|g' main.py || true
        sed -i 's|C:/Windows/Fonts/msyh.ttc|fonts/NotoSansCJK.ttc|g' main.py || true
        
        # 4. 下载字体
        wget -O fonts/NotoSansCJK.ttc https://github.com/googlefonts/noto-cjk/raw/main/Sans/OTF/Chinese%20\(Simplified\)/NotoSansCJKsc-Regular.otf || true
        
        # 5. 开始打包 (去掉重试逻辑，让错误直接暴露)
        buildozer android debug
      shell: bash
    
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: powersafety-apk
        path: bin/*.apk
        retention-days: 30
